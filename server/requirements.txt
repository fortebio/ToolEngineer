# Cài trên server:  ~/fbt_server/venv/bin/pip install -r requirements.txt
fastapi==0.139.0
uvicorn[standard]==0.51.0
psycopg[binary]==3.3.4
# httpx chỉ cần cho test API (fastapi.testclient); server không bắt buộc
httpx
