# 2026-09-23 — Deploy B1–B3 + ống chuẩn `/calib/*` + ghép 3 route `/logs` chỉ có trên box

## Phát hiện: code chỉ có trên box

`check_deploy.py` trước deploy báo prod 39 route / local 53, mục **chỉ có ở prod** có 3 route:
`GET /logs`, `PUT /logs/{filename}/status`, `DELETE /logs/{filename}` (hộp thư log CSKH cho kỹ thuật,
gác `ota_admin`). Không nằm ở nhánh/commit/stash/worktree/cây chưa commit nào → `deploy.ps1` sẽ xoá mất.

- Máy `Admin` chưa SSH được box: khoá `~/.ssh/id_ed25519_fbt` chưa có trong `authorized_keys` →
  người dùng thêm qua Cockpit.
- Kéo về: `scp -O "fbt-server:~/fbt_server/app/*.py" C:\Users\Admin\Documents\box_app\`.
- So từng file (bỏ CR): box chỉ hơn local ở **khối log của `main.py`** (`_LOG_STATUSES`, cache
  `_LOG_META_CACHE` theo mtime/size, `_log_paths`, 3 route). Mọi khác biệt còn lại (`config.py`,
  `logic.py`, `ota.py`, phần OTA/devices của `main.py`) là bản CŨ mà B1–B3 đã viết lại; box chưa có `calib.py`.
- Ghép khối log vào `app/main.py` (khớp từng dòng với box), thêm 2 test vào `tests/test_api.py`
  (gác token, lọc trạng thái + `counts`, ghi `status*` giữ `text` cuối file, xoá) → **132 passed**.
  Commit `b927a94`. `check_deploy.py` sau ghép: *chỉ có ở prod* = rỗng.

## Deploy

- `deploy.ps1 -Server -Target fbt-server` (host mặc định `engineer@fbt.basa-luma.ts.net` không khớp
  `Host fbt-server` trong `~/.ssh/config` nên không dùng khoá → `Permission denied`; phải truyền `-Target fbt-server`).
- Sao lưu: **`~/fbt_server/app.bak.20260923-151614`**. Chép 10 file `app/*.py` (lần đầu có `calib.py`)
  + `scripts/migrate_ota.py`; md5 box == local; `IMPORT_OK` bằng venv box.
- Restart `fbt-receiver` (sudo): người dùng chạy tay — AI bị classifier chặn.
- 15:17:54 bản mới lên sạch (mọi request 200) nhưng 15:19:10 **lệnh rollback bị dán kèm** (chat in cả hai
  lệnh liền nhau) → xoá `app.bak.20260923-151614`, về code 09-18. Bài học: chỉ đưa lệnh rollback KHI cần,
  không in chung khối với lệnh restart. Chép lại 15:25 → bak **`app.bak.20260923-152540`**, `IMPORT_OK`.
- Env: không bắt buộc thêm gì (`FBT_CALIB_DIR` mặc định `~/fbt_server/calib`, tự tạo; `OTA_REQUIRE_TAG`
  cũ "1"/rỗng vẫn cùng nghĩa).

## Kiểm sau restart (15:27:29)

- `check_deploy.py` → prod 56 / local 56 route, **openapi giống hệt: CÓ** (exit 0).
- md5 `main.py`/`logic.py`/`ota.py`/`config.py` trên box == cây làm việc.
- Route mới thiếu token → 401: `/ota/rapidplus/progress`, `/calib/batches`, `/logs`.
- Journal 5 phút sau: 0 Traceback; request 200/304 bình thường.
- Web `/app/` CHƯA build lại (`build/web_prod` chưa có) → tab ống chuẩn chưa hiện trên web; việc tiếp theo.

## Deploy web `/app/` (15:31)

- `flutter build web --release --base-href /app/ --output build/web_prod` (Flutter 3.44.1 máy Admin, 74 s);
  bundle có `calib/batches`, không nhúng `127.0.0.1:8080`/`localtok`.
- `deploy.ps1 -Web -Target fbt-server` (tự sao lưu `web.bak.*`, giữ 3 bản).
- Kiểm: `index.html` → `flutter_bootstrap.js` → `main.dart.js` md5 `5fc31789…` == `build/web_prod`;
  `hub.fortebio.tech/app/` trả `Cache-Control: no-cache`, `cf-cache-status: DYNAMIC`.
- Tab mới: **Hiệu chuẩn** (`nav.calib`) — chỉ hiện cho `isStaff || isFactory` (`canSeeCalib`).
