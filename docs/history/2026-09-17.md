# 2026-09-17

## Plan: API cho RAPID ERP kéo dữ liệu từ Engineer Server

- Thêm `docs/plan/erp-feed-engineer-server.md` (xuyên phần: `server/` + `system/` + ERP `fbterp` ngoài
  repo). Đọc cả hai repo trước khi lập: firmware đã POST kết quả đo thẳng vào ERP
  (`postJsonToAllTargets`: GAS + Engineer + ERP cùng payload) nên feed mới **không** đồng bộ lại
  kết quả; nội dung feed = hồ sơ ATE sản xuất, trạng thái fleet/firmware tự khai + OTA, log CSKH,
  đường backfill phiên đo. ERP kéo (Celery beat, pattern `sheets_sync`), token chỉ-đọc riêng
  `ERP_READ_TOKENS`, route `/erp/v1/*` chỉ GET (gotcha POST catch-all).
- Đề xuất chốt nguồn sự thật danh tính máy (điểm "P2" trong `products.yaml`): ERP = danh tính
  thương mại, Engineer = trạng thái kỹ thuật. Chưa sửa code/registry — chờ trả lời §10 của plan.
- `system/products.yaml`: thêm dòng chú thích trỏ plan tại `external_systems.erp`.
